name: CI/CD Pipeline - Movies DB App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Login to Azure Container Registry
        run: az acr login --name devopsacr707

      - name: Build Docker image
        run: docker build -t devopsacr707.azurecr.io/movies-db:latest .

      - name: Push image to ACR
        run: docker push devopsacr707.azurecr.io/movies-db:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Get AKS credentials
        run: az aks get-credentials --resource-group devops-rg --name devops-aks --overwrite-existing

      - name: Deploy to AKS
        run: |
          kubectl set image deployment/devops-static-site devops-static-site=devopsacr707.azurecr.io/movies-db:latest
          kubectl rollout status deployment/devops-static-site
